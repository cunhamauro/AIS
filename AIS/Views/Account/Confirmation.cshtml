@model ConfirmationViewModel
@{
    ViewData["Title"] = $"{Model.Title}";
}

<div class="confirm-container">
    <h1 class="confirm-title">@Model.Title</h1>
    <p class="confirm-text" style="margin-bottom: -0.5rem">@Model.Message</p>
</div>
